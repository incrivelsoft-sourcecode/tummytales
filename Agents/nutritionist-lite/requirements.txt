#requirements.txt for Nutritionist Lite Agent

# Core FastAPI (keep same)
fastapi>=0.104.1
uvicorn>=0.24.0
pydantic>=2.5.1
python-dotenv>=1.0.0
httpx>=0.24.0

# Database (keep same)
motor>=3.0.0
pymongo>=4.0.0
pinecone-client>=3.0.0

# API clients (lightweight replacements)
openai>=1.0.0
anthropic>=0.54.0

# Minimal LangChain (keep only essentials - no heavy dependencies)
langchain-core>=0.1.0
langchain-anthropic>=0.1.0
langchain-pinecone>=0.1.0

# REMOVED HEAVY PACKAGES (saves ~9GB):
# sentence-transformers>=2.2.0    # -5.8GB (torch, nvidia, cuda)
# crewai==0.120.1                # -841MB (heavy framework)
# langchain>=0.1.0               # -152MB (full ecosystem)
# langchain-community>=0.0.16    # Part of heavy langchain
# crewai-tools>=0.17.0           # Part of crewai
